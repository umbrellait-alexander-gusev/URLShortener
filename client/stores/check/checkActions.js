import { createAction } from 'redux-act';

export const checkActionsLoad = createAction('Check actions load');
export const checkActionsSuccess = createAction('Check actions success');
export const checkActionsError = createAction('Check actions error');
