import { createAction } from 'redux-act';

export const checkActionsLoad = createAction('checkActionsLoad');
export const checkActionsSuccess = createAction('checkActionsSuccess');
export const checkActionsError = createAction('checkActionsError');
